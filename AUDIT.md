# 代码质量与规格完成性审计

审计日期：2026-07-27
审计对象：青龙重构版 `3.0.2-ql.14`、浏览器扩展 `3.0.1`、上游源码 `3.0.2`

## 结论

当前版本适合作为公开的 **Beta/实验性版本**，不应标记为生产稳定版。

基础架构、凭据隔离、青龙配置、主要 HTTP 任务、双账号实测、离线测试和许可证保留
已经完成；但 Microsoft Rewards 使用未公开接口。必须依赖完整浏览器 DOM 的特殊
Punch Card 和部分 Quiz 页面交互没有做虚假兼容。

## 规格矩阵

| 原版能力 | 青龙版状态 | 说明 |
| --- | --- | --- |
| 多账号 | 已完成 | JSON 数组，账号级 Cookie jar 与状态文件 |
| OAuth 刷新 | 已完成 | 支持 refreshToken、一次性 authCode、令牌轮换 |
| PC 签到 | 已实号验证 | 旧接口不可用时安全跳过 |
| App 签到 | 已实号验证 | DAPI Bearer Token；账号身份预检，余额未变不计成功 |
| 新闻阅读 | 已实号验证 | 查询进度、每日最多 10 次、以 DAPI 余额变化确认入账 |
| 活动卡片 | 部分完成并实号验证 | 新版 Server Action；DOM 专属卡片不支持 |
| 每日活动 | 部分完成 | 可发现的 dailySet 卡片走 HTTP 上报 |
| Quiz | 部分完成 | 可上报卡片；需要真实页面答题的变体不保证 |
| 拼图/Punch Card | 部分完成 | 普通卡片可上报；多步 DOM 点击不支持 |
| PC 搜索 | 已实号验证 | 搜索页面、reportActivity、余额与面板双重确认 |
| 移动搜索 | 已实号验证 | 移动 UA、ncheader、reportActivity；新版国区计入 PC 合并配额 |
| 第三方热搜 API | 已完成 | 两个可用源随机选择、结构校验、无账号 Cookie、失败回退本地词库 |
| 二次扫描 | 已完成 | 搜索任务后重新发现活动 |
| 连签 | 只读完成 | 查询天数；视觉搜索等页面点击不模拟 |
| 待领取积分 | 已完成 | 识别 pointClaim、Server Action、余额与剩余值复核 |
| 通知 | 已完成 | 复用青龙 `sendNotify.js` |
| 国区锁定 | 已完成 | 可配置；解析失败时告警并继续 |
| dry-run | 已完成 | 不刷新 OAuth、不写令牌、不提交任务 |
| 浏览器同步扩展 | 已完成 | Cookie、OAuth Token、青龙 OpenAPI 同步 |

## 质量检查

| 检查项 | 结果 |
| --- | --- |
| Node.js 语法检查 | 通过 |
| Node.js 18/20/22 CI 配置 | 已加入 |
| 离线单元测试 | 36 项，通过 |
| Cookie 跨域隔离 | 有测试覆盖 |
| HTTP 重定向与 Set-Cookie | 有测试覆盖 |
| dry-run 无 OAuth 写操作 | 有测试覆盖 |
| 上游源码完整性 | SHA-256 固定并测试 |
| 扩展权限边界 | Bing/Login 固定权限，青龙 origin 运行时授权 |
| 扩展凭据存储 | Token 仅 session storage；青龙密钥不保存 |
| 真实账号集成测试 | 未执行 |
| Microsoft 接口兼容性 | 无稳定性保证 |

## 本轮修复

1. 修复 `dry-run` 仍刷新 OAuth 并写入状态文件的问题；
2. 补回原版的活动二次扫描；
3. PC 签到不再把无法解析或未确认的响应当成成功；
4. 活动校验令牌固定从 Rewards 域获取；
5. OAuth 优先使用轮换后的状态令牌，并在失败时回退到环境变量令牌；
6. 新增浏览器同步扩展，OAuth Token 仅会话存储并支持青龙 OpenAPI；
7. 增加 CI、许可证 NOTICE、安全说明和规格矩阵。
8. 移除对旧版 `tifacfaatcs` Cookie 的错误硬性要求，检查 `_U`，并兼容
   `.MSA.Auth` 与新版 `_C_Auth` Rewards 登录字段。
9. 恢复第三方热搜多源，使用独立无 Cookie 客户端并在异常时回退本地词库；
10. 修正扩展发布包目录结构，确保 ZIP 解压根目录直接包含 `manifest.json`。
11. 将临时 action 弹窗改为固定独立小窗，并按用户选择持久化青龙连接信息和同步结果。
12. 恢复原版的 `earn` 页面优先解析路径，避免 `getuserinfo` 单接口 401 导致整号误判。
13. 分离 Bing 搜索与 Rewards Cookie，OAuth 强制选择账号并绑定 Cookie 会话指纹。
14. 修复 OAuth 状态查询遗漏账号指纹的问题，并消除快速 SSO 回调与状态写入的竞态。
15. 将扩展重构为 session 级多账号仓库，为每个账号独立绑定 Cookie、搜索 Cookie
    和 refreshToken，并支持一次同步完整账号数组及编号变量。
16. 搜索积分改为以余额变化和最终仪表板确认，不再把 `RewardsIncrement` 字段直接
    当作已入账；未确认活动卡片增加持久化冷却，避免定时任务重复提交。
17. 对齐新版 Rewards 页面 `reportActivity` Server Action，并以二次读取仪表板
    作为活动完成依据；账号2实测活动余额增加 15 分且卡片状态转为完成。
18. 增加首页待领取积分识别和 `reportClaimAllPoints` Server Action；仅在待领取
    积分大于 0 时提交，并通过余额及剩余待领取积分进行二次确认。
19. 默认青龙计划从每 20 分钟一次调整为每天 09:17 一次，并补充单文件订阅、
    仓库拉取、自动创建任务、首次 dry-run 和生产启用教程。
20. 增加独立 `mobile` 搜索模块；账号2在线验证余额增加 3 分，面板合并搜索进度
    从 15/60 增至 18/60，默认每轮仅执行 3 次并沿用随机搜索间隔。
21. 将总任务拆为 7 个带独立 Cron 元数据的入口脚本；仓库拉取后由青龙自动创建
    签到、阅读、活动、电脑搜索、移动搜索、连签和领取任务，共享核心不再重复调度。
22. 修复 Rewards 首页重定向到 `/dashboard` 后，领取请求仍提交旧根路径导致
    HTTP 405；领取动作现在绑定实际页面 URL 并校验目标域名。
23. 移动端签到和阅读改为以 DAPI 余额变化确认积分，不再把接口返回值误报为入账。
24. OAuth 执行前同时核对 Cookie 余额、DAPI 余额和匿名账号标识；发现多个备注
    使用同一 Token 时阻止串号执行。扩展授权后立即读取身份标识并拒绝重复绑定。

## 已知技术债

- HTTP 客户端没有通用自动重试，避免对非幂等领奖/签到请求造成重复提交；
- Cookie jar 是任务级实现，不覆盖 RFC 6265 的全部边界行为；
- 未公开 Microsoft API 可能在无通知情况下更改响应结构；
- 缺少使用专用测试账号的在线契约测试；
- 状态文件没有进程锁；同一账号不应并发执行多个青龙任务；
- 浏览器扩展当前以源码方式加载，尚未发布到 Chrome/Edge 扩展商店；
- 第三方热搜服务可能变更、限流或记录请求来源，不能视为降低账号风控的保证。

## 发布建议

- GitHub 首个版本标记为 `beta` 或 `experimental`；
- 不创建声称“防封号”的保证性描述；
- Release 中附上上游来源、SHA-256、许可证和已知限制；
- 浏览器扩展 OAuth 与青龙同步应先以 Beta 发布并进行人工授权测试。
